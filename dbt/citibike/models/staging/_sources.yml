sources:
  - name: de_citibike_raw
    description: "Citibike source dataset"
    schema: "{{ env_var('GCP_PREFIX') }}_citibike_raw"
    tables:
      - name: trips
        description: "Citibike table"
        identifier: trips
